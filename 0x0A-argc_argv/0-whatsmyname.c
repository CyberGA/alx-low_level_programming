#include <stdio.h>

/**
 * main - prints name of the program
 * @argc: argument count
 * @argv: argument vector
 * Return: Success (0)
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
