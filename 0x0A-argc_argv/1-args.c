#include <stdio.h>

/**
 * main - prints argument count
 * @argc: argument count
 * @argv: argument vector
 * Return: Success (0)
 */
int main(int argc, char (void) *argv[])
{
	printf("%d\n", argc);
	return (0);
}
