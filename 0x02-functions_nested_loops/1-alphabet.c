#include "main.h"

/**
 * main - prints lowercase alphabets;
 *
 * Return: 0
 */
int main(void)
{
	char alphabet;

	for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
	{
		_putchar(alphabet);
	}
	_putchar('\n')'
	return (0);
}
